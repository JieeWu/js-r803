const start = document.getElementById('start')

start.addEventListener('click',function(){
    const rand = Math.floor(Math.random() * 6) + 1

    // const diceWords = ['one', 'two']

    // let diceType = ''

    // switch(rand){
    //     case 1 :
    //         diceType='one'
    // }
})